.. currentmodule:: cuda.core.experimental

``cuda.core`` v0.2.0 Release notes
==================================

Breaking changes
----------------

- Change ``__cuda_stream__`` from attribute to method
- The positional arguments to :meth:`~launch` are swapped, so the signature is now ``(config, kernel, *kernel_args)``