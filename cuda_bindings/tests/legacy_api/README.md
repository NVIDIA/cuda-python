This directory contains tests for the legacy (non-v2-prefixed) APIs for
`driver`, `runtime` and `nvrtc`.  These were created from snapshots of the
original version, and then the originals were updated to use the new API.  Do
not add to the tests here, and do not update them unless necessary to fix a bug.
